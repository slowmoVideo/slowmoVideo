language: cpp

compiler:
  - gcc

before_install:
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start

install:
  - sudo apt-get install -y qt5-default qttools5-dev-tools #install necessary Qt files

before_script:
  - mkdir build
  - cd build
	- cmake ../slowmoVideo/src -DOpenCV_DIR=/home/vbr/Documents/ocv31/share/OpenCV


script:
				make

